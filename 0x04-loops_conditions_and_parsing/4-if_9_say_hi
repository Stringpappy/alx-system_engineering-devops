#!/usr/bin/env bash
#script Display "best School" 10 times using while loop

iterator=0
until [ $iterator -lt 10 ]
do
	if [ $iterator -eq 10 ]
	then
		echo "Hi"
	fi
	echo "Best School"
	(( iterator++ ))
done

